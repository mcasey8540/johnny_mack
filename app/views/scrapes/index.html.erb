<%= button_to "New Scrape", new_scrape_path, class: "btn btn-info" %>

<table class= "table table-striped table-bordered" >
		<tr>
			<th>ID</th>
			<th>Date</th>
		<tr>

	<% @scrapes.each do |t| %>
		<tr>
			<td><%= t.id %></td>
			<td><%= t.created_at %></td>
		<tr>	
	<% end %>
</table>


